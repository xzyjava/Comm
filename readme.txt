###这只是一部分
	*作为一个新手，这里面要改的东西还很多，比如异常处理。。。懒了点，我会改的。。好像有点水。
###整体叙述一下：
	*A:zigbee（硬件不是我做的）通过串口输入数据,先用Comm包写这个串口通信的部分;	
	*B:之后再用JDBC把输入的数据处理之后存到MySQL里;
	*C:然后搭建PHP环境，把这个文件放在apache站点里，用来映射到外网;
	*D:映射部分用的是nat123;
	*E:最后用app抓取数据显示数据到app端。（我相信肯定有很多比这简单又方便的方法，但是，我不会）
		*1.这里的Comm文件就是串口通信部分，内详。
###